{
  "name": "event-os",
  "private": true,
  "description": "Event OS monorepo — We Decor Phase 1",
  "scripts": {
    "api:install": "pnpm --dir apps/api install",
    "api:dev": "pnpm --dir apps/api start:dev",
    "api:build": "pnpm --dir apps/api build",
    "api:test": "pnpm --dir apps/api test",
    "api:lint": "pnpm --dir apps/api lint",
    "web:dev": "pnpm --dir apps/web dev",
    "web:build": "pnpm --dir apps/web build",
    "web:lint": "pnpm --dir apps/web lint"
  },
  "engines": {
    "node": ">=20"
  }
}
