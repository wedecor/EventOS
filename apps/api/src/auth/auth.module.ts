import { Module } from '@nestjs/common';

/**
 * Authentication module placeholder.
 * Passport JWT strategy and guards are implemented in Sprint 1 (ADR-008).
 * Do not add business auth endpoints here until the Lead/Auth sprint begins.
 */
@Module({})
export class AuthModule {}
